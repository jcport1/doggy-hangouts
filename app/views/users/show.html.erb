<h1> Welcome, <%= @user.name %></h1>

<h4>My pet(s):</h4> 
<ul> 
<% @user.pets.each do |p| %>
     <li><%= p.name %></li>
     <% end %>
 </ul>

<h4>Requests Received</h4>

<i>pending approval </i>

    <ul> 
    <% @user.authored_listings.requests.each do |r| %>
        <li>Message: <%= r.message %> - Received on <%= r.created_at %></li>
        <% end %> 


<h4>Requests Sent</h4> 
<ul> 

<% @user.requests.each do |p| %>
    <li>Message: <%= p.message %> - Sent on: <%= p.created_at %> </li>
    <% end %>
</ul> 


<%= link_to "Add a Pet", new_pet_path %> 